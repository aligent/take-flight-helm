apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: takeflight-production
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: takeflight
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/takeflight
    repoURL: https://github.com/aligent/take-flight-helm.git
    targetRevision: main
    helm:
      valueFiles:
        - values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true